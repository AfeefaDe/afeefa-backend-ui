{{outlet}}

<div class="row">
  <div class="col s12 m8 push-m2">
    <div class="card">
      <div class="card-content black-text">
        {{entry-detail model=model}}

        <p><b>users:</b>
          <ul>
            {{#each model.users as |instance|}}
            <li>
              {{instance.forename}} {{instance.surname}}
              <a {{action 'navigateToSingle' instance}}>Show details</a>
            </li>
            {{/each}}
          </ul>
        </p>
        <div class="card-action">
          {{#link-to "protected.editorga" model.id}}Bearbeiten{{/link-to}}
          <a {{action 'delete'}}>Löschen</a>
        </div>
      </div>
    </div>
  </div>
</div>
