{{outlet}}

<div class="row">
    <div class="col s12 m6 push-m3">
    <h3>Seach</h3>
    <form {{action 'search' on='submit'}}>
      <div class="input-field">
        {{input value=term id="term" type="text" class="validate"}}
        <label for="term">Search Term</label>
      </div>
      <button class="btn waves-effect waves-light" type="submit" style="margin-top:2em;">Search</button>
    </form>
  </div>
</div>