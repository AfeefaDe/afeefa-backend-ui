{{todo-list title="Things to do" todos=model.orgas}}

{{#if session.isAuthenticated}}
  logged in as id: {{session.currentUser}}
  {{#paper-button raised=true action="logout"}}Logout{{/paper-button}}
{{else}}
  {{#link-to 'login'}}Login{{/link-to}}
{{/if}}
{{outlet}}
