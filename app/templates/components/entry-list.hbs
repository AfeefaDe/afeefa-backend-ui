{{yield}}

<ul class="entryList">
    {{#each (limit-array sortedInstances limit) as |instance|}}
      {{#if attributes}}
        {{entry-list-item instance=instance attributes=attributes}}
      {{else}}
        {{entry-list-item instance=instance}}
      {{/if}}
    {{/each}}
</ul>
