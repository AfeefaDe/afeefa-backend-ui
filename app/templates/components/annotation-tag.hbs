{{yield}}
<p class="annotationTag" title="Durch das Bearbeiten des Eintrags können Anmerkungen entfernt und hinzugefügt werden.">
  {{instance.title}}
  {{#if onDelete}}
    <a {{action 'removeAnnotation'}} class="annotationTag__icon">
      <i class="material-icons">close</i>
    </a>
  {{/if}}
</p>
