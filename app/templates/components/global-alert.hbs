{{yield}}
<div {{action 'closeAlert'}} class="alert {{if isError 'alert--error' 'alert--success'}} {{if visible 'alert--visible' 'alert--invisible'}}">
  <div class="alert__closeIcon">
        <i class="material-icons">close</i>
  </div>
  <div class="alert__stateIcon">
    <i class="material-icons">{{alertTypeIcon}}</i>
  </div>
  <div class="alert__content">
    <p class="alert__contentTitle">{{title}}</p>
    <p class="alert__contentDescription">{{description}}</p>
  </div>
</div>
