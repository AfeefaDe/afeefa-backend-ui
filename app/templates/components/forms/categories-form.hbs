{{yield}}

<select class="browser-default categoriesForm" onchange={{action "selectCategory" value="target.value"}}>
  <option selected value="-1">Keine Kategorie ausgewählt</option>
  {{#each possibleCategories as |category|}}
    {{#if (eq instance.id category.id)}}
      <option selected value="{{category.id}}">{{category.title}}</option>
    {{else}}
      <option value="{{category.id}}">{{category.title}}</option>
    {{/if}}
  {{/each}}
</select>
