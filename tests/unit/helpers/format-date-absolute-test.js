import { formatDateAbsolute } from 'afeefa-backend-ui/helpers/format-date-absolute';
import { module, test } from 'qunit';

module('Unit | Helper | format date absolute');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatDateAbsolute(new Date());
  assert.ok(result);
});
